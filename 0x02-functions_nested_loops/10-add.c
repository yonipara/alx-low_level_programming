#include "main.h"
/**
 * add -> adds two integers
 * @a: is the first argument add takes
 * @b: is the second argument add takes
 * Return: the sum of the two integers
 */
int add(int a, int b)
{
return (a + b);
}

