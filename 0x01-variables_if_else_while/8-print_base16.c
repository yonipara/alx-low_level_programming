#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char a = 48;
int A = 97;

for (a = 48; a <= 57; a++)
{
putchar(a);
}

for (A = 97; A <= 102; A++)
{
putchar(A);
}
putchar('\n');
return (0);
}
