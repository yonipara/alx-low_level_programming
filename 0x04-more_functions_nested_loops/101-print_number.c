#include <stdio.h>
#include "main.h"
/**
 * print_number -> prints all the decimal digits of an integer number
 *
 */
int main(void)
{
print_number(98);
return (0);
}
void print_number(int n)
{
printf("size of n: %lu\n", sizeof(n));
}
